using Emp_Intranet_UI.API;
using Emp_Intranet_UI.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Web;

namespace Emp_Intranet_UI.Controllers.AuthHelpers
{   
    public class ProfileLoader : IProfileLoader
    {
    //    UserEndPoint _user;

    //    public ProfileLoader(UserEndPoint user)
    //    {
    //        _user = user ?? throw new ArgumentNullException(nameof(user));
    //    }
    //    public async Task<ProfileModel> LoadProfile(int userID)
    //    {
    //        if (userID < 0)
    //        {
    //            throw new ArgumentNullException(nameof(userID));
    //        }

    //        var profile = await _user.GetProfileByUser(userID);
    //        return profile;

    //    }
    }
}